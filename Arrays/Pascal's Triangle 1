class Solution {
public:
    int pascalTriangleI(int r, int c) {
        long long result = 1;
        for(int i = 0; i < c-1; i++) {
            result = result * (r-1-i);
            result = result / (i+1);
        }
        
        return (int)result;
    }
};